# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LuohenWeather (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LuohenWeather (iOS)
  pod 'AMapSearch'
  pod 'AMapLocation'
  pod 'Moya'
  pod 'Alamofire'
  pod 'HandyJSON'
  pod 'SwifterSwift'
  
end

target 'LuohenWeather (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LuohenWeather (macOS)

end
